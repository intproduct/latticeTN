# Stage 5B Two-Site AD Local-Tensor Optimization Report

Generated by `scripts/run_ad_two_site.py`.

## Mainline statement

> _The PRIMARY optimization path is two-site AD local-tensor optimization: contract two adjacent MPS site tensors into a single trainable two-site center tensor Theta, train it as a torch Parameter on the differentiable local Rayleigh quotient E(Theta)=<Theta|H_eff|Theta>/<Theta|Theta> via loss.backward() + a torch optimizer step, then split Theta back into two site tensors by SVD with optional max_bond_dim/cutoff truncation. Sweep left-to-right then right-to-left. SVD/QR/canonicalization are OPTIONAL post-step split / compression / stabilization, NEVER the solver. DMRG/Lanczos/eigh are classical reference baselines ONLY and are NOT used in the AD optimization path._

### Stabilization policy (SVD/QR are not the solver)

This stage's mainline is **two-site AD local-tensor optimization**: train one two-site center tensor Theta at a time on the differentiable local Rayleigh quotient (`loss.backward()` + torch optimizer step), then split Theta back into two site tensors by SVD with optional `max_bond_dim`/`cutoff` truncation. **SVD / QR / canonicalization are OPTIONAL post-step split / compression / inter-bond gauge fixing ONLY — they are NOT the solver** and never appear in the loss path. Re-canonicalizing the chain at the next bond is gauge fixing (under `no_grad`, mutating detached data). No Lanczos / `eigh` / classical DMRG is used in the AD optimization path.

## Convention

`H = J * sum_i S_i.S_{i+1}, S = sigma/2, J = 1.0, open boundary, complex128`
- dtype: `torch.complex128`
- optimizer: `LBFGS, lr=1.0, per-bond local_steps=20, num_sweeps=4, max_bond_dim=8 (optional growth)`

## Exact comparison (two-site AD, N<=6, LBFGS)

| N | chi | sweeps | local_steps | max_bond_dim | exact E0 | two-site AD final E | abs err | tol | below ground | energy decreased |Theta grad not-None | Theta grad finite | runtime_s | pass |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|:---:|:---:|:---:|:---:|---:|:---|
| 4 | 8 | 4 | 20 | 8 | -1.6160254038 | -1.6160254036 | 1.82e-10 | 1e-08 | False | True | True | True | 2.73 | PASS |
| 6 | 8 | 4 | 20 | 8 | -2.4935771339 | -2.4935771330 | 8.39e-10 | 1e-05 | False | True | True | True | 0.30 | PASS |

## One-site AD comparison (N=6)

- one-site AD local (LBFGS, center sweep) final E: `-2.4935771330`
- two-site AD final E: `-2.4935771330`
- exact E0: `-2.4935771339`
|two-site - one-site AD|: `5.23e-11`

Both are AD strategies on differentiable Rayleigh quotients; two-site AD additionally allows bond growth / truncation at each split and reaches the same variational minimum.

## Global AD-MPS comparison (N=6)

- global AD-MPS (Adam, all tensors) final E: `-2.4935624158`
- two-site AD final E: `-2.4935771330`
- exact E0: `-2.4935771339`
|two-site - global AD|: `1.47e-05`

## DMRG reference comparison (N=6)

- classical DMRG (dense reference) final E: `-2.4935771339`
- two-site AD final E: `-2.4935771330`
- exact E0: `-2.4935771339`
|two-site AD - DMRG|: `8.39e-10`

DMRG/Lanczos/eigh are classical baselines and are **not** part of the AD optimization path; they are run here only to confirm the two-site AD solver reaches the same variational minimum.

## Energy history (sampled, per sweep)

N=4 (initial E = -0.078104):

| sweep | direction | energy after | grad norm | max bond | max trunc |
|---:|:---:|---:|---:|---:|---:|
| - | init | -0.0781043985 | 0.000e+00 | 4 | 0.00e+00 |
| 0 | right | -1.6160254036 | 2.079e-07 | 4 | 0.00e+00 |
| 1 | left | -1.6160254036 | 2.079e-07 | 4 | 0.00e+00 |
| 2 | right | -1.6160254036 | 2.079e-07 | 4 | 0.00e+00 |
| 3 | left | -1.6160254036 | 2.079e-07 | 4 | 0.00e+00 |

N=6 (initial E = -0.386218):

| sweep | direction | energy after | grad norm | max bond | max trunc |
|---:|:---:|---:|---:|---:|---:|
| - | init | -0.3862184141 | 0.000e+00 | 8 | 0.00e+00 |
| 0 | right | -2.4935771330 | 8.095e-08 | 8 | 0.00e+00 |
| 1 | left | -2.4935771330 | 9.403e-08 | 8 | 0.00e+00 |
| 2 | right | -2.4935771330 | 8.095e-08 | 8 | 0.00e+00 |
| 3 | left | -2.4935771330 | 9.403e-08 | 8 | 0.00e+00 |

## Gradient check

Loss = `<Theta|H_eff|Theta>/<Theta|Theta>` with only the two-site center tensor Theta trainable (fully differentiable einsum on Theta and the frozen constant MPO environments; no `detach()/.data/no_grad()/unnecessary .item()` in the loss path). After `loss.backward()` Theta receives a non-None, finite gradient; the frozen environment tensors receive None. The SVD split / QR re-canonicalization live only in post-step / preprocessing helpers under `no_grad`.

## Bond growth / compression

At each bond the optimized Theta is split by SVD into two site tensors. With `max_bond_dim` set, the kept bond is `min(max_bond_dim, #singular values above cutoff)`; without a cap the bond may grow up to `min(l*d, d*r)` (full two-site growth). The discarded weight is recorded as the per-sweep truncation error. This is compression / stabilization, NOT the optimizer.

| N | final bond dims | max bond |
|---:|:---|---:|
| 4 | [2, 4, 2] | 4 |
| 6 | [2, 4, 8, 4, 2] | 8 |

## Truncation errors

N=4 per-sweep max truncation errors:

| sweep | direction | max truncation | per-bond truncations |
|---:|:---:|---:|:---|
| 0 | right | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00 |
| 1 | left | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00 |
| 2 | right | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00 |
| 3 | left | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00 |

N=6 per-sweep max truncation errors:

| sweep | direction | max truncation | per-bond truncations |
|---:|:---:|---:|:---|
| 0 | right | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00 |
| 1 | left | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00 |
| 2 | right | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00 |
| 3 | left | 0.00e+00 | 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00, 0.00e+00 |

## Overall pass/fail

**pass: `True`**

| check | passed |
|---|:---|
| ad_two_site_n4_vs_exact | True |
| ad_two_site_n6_vs_exact | True |
| energy_decreased_n4 | True |
| energy_decreased_n6 | True |
| grad_check_n4 | True |
| grad_check_n6 | True |
| close_to_one_site_ad | True |
| close_to_global_ad | True |
| close_to_dmrg_reference | True |
| no_below_ground | True |

## Known limitations

- Two-site AD trains one two-site block at a time; its per-bond conditioning benefits from LBFGS (the local Rayleigh problem is near-quadratic in Theta).
- SVD split / QR re-canonicalization appear only as post-step split / compression / inter-bond gauge fixing (under no_grad, mutating detached data); they are NOT the optimizer and never appear in the loss path.
- Bond growth is optional (max_bond_dim cap); without a cap the bond can grow up to min(l*d, d*r) at each bond, which is the full two-site DMRG-style growth but driven by gradient descent on Theta.
- Adam is supported but slower to converge per-bond than LBFGS; LBFGS is the default/smoke optimizer.
- DMRG/Lanczos/eigh are classical baselines, NOT the AD mainline; they are run only for comparison.
