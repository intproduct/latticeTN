# Stage 7B Model Builder + Benchmark Registry Report

Generated by `scripts/run_model_builder_benchmark.py`.

## Mainline statement

> _This benchmark evaluates the AD mainline solver (global AD-MPS) across the registered 1D model presets via the unified model builder + benchmark registry, on CPU and (opt-in) a V100/TITAN V GPU. Exact diagonalization is a reference baseline ONLY. The AD loss path is unchanged; Stage 7B adds only the model/MPO builder and benchmark registry layers._

Stage 7B is a **model/MPO construction layer, NOT a new solver**. The AD solver is still the differentiable Rayleigh quotient + autograd + torch optimizer. SVD/QR/canonicalization remain auxiliary stabilization. Exact/DMRG/Lanczos remain reference baselines. GPU timing from Stage 7 uses the V100/TITAN V unified selector.

## Device info

- PyTorch version: `2.10.0+cu128`
- CUDA available: `True`
- GPU device count: `3`
- GPU name: `Tesla V100-SXM2-16GB`
- Device used: `cuda:0`
- dtype: `torch.complex128`
- `LATTICETN_RUN_GPU=1`: `True`
- Allowed GPU filters: `['V100', 'TITAN V', 'Titan V']`

**GPU RAN** on `cuda:0` (`Tesla V100-SXM2-16GB`).

## CPU/GPU comparison per model

| model | N | chi | device | device_name | final E | exact error | runtime_s | speedup | below ground |
|---|---:|---:|:---:|:---:|---:|---:|---:|---:|:---:|
| heisenberg | 4 | 4 | cpu | cpu | -1.6159638812 | 6.15e-05 | 2.002 | — | False |
| heisenberg | 4 | 4 | cuda:0 | Tesla V100-SXM2-16GB | -1.6159638812 | 6.15e-05 | 1.777 | 1.127e+00 | False |
| heisenberg | 6 | 8 | cpu | cpu | -2.4924183647 | 1.16e-03 | 1.066 | — | False |
| heisenberg | 6 | 8 | cuda:0 | Tesla V100-SXM2-16GB | -2.4924183647 | 1.16e-03 | 2.102 | 5.071e-01 | False |
| spinless_fermion_tv | 4 | 4 | cpu | cpu | -2.4677791160 | 1.50e-04 | 0.712 | — | False |
| spinless_fermion_tv | 4 | 4 | cuda:0 | Tesla V100-SXM2-16GB | -2.4677791160 | 1.50e-04 | 1.419 | 5.013e-01 | False |
| spinless_fermion_tv | 6 | 8 | cpu | cpu | -3.8291809684 | 3.33e-03 | 1.064 | — | False |
| spinless_fermion_tv | 6 | 8 | cuda:0 | Tesla V100-SXM2-16GB | -3.8291809684 | 3.33e-03 | 2.115 | 5.031e-01 | False |

Speedup = CPU runtime / GPU runtime. **The GPU is NOT required to be faster**: small systems are overhead-dominated.

## Model builder coverage

| preset | statistics | dense ref | MPO dense | native energy |
|---|:---:|:---:|:---:|:---:|
| `heisenberg_model` | boson (S=sigma/2) | `operators.heisenberg_dense` | `MPO.generate_heisenberg` | aligns |
| `spinless_fermion_tv_model` | fermion (JW) | `operators.spinless_fermion_dense` | `MPO.generate_spinless_fermion` | aligns |

Both presets' dense/MPO/native energies align to the existing references (Stage 1/7A). The fermion preset keeps the JW parity string; it does NOT degrade to hard-core boson.

## Overall pass/fail

**pass: `True`**

## Known limitations

- Stage 7B ships two presets (Heisenberg, spinless fermion t-V). Hubbard / XXZ / TFI / long-range are future work.
- Model/MPO construction layer only; no new solver. The AD mainline is unchanged.
- No TDVP, no finite-temperature, no Hubbard, no graded fermionic tensors.
- Small systems only (N=4/6, chi=4/8); large-N scaling is out of scope.
- GPU is opt-in (LATTICETN_RUN_GPU=1) and selects only V100/TITAN V; clean-skips otherwise.
