# Claude Progress Log: Stage 2 Benchmark

Append checkpoint entries here.

## Template

```text
Date/time:
Stage:
Files changed:
Commands run:
Results:
Current failing item:
Next action:
Notes:
```

## Checkpoint 2026-07-01 — Stage 2 complete

```text
Date/time: 2026-07-01
Stage: Stage 2 Heisenberg MPS benchmark tool (final)
Files changed:
  - latticetn/observables.py (NEW): dense + MPS observable API
        dense_expect_local, dense_expect_two_site, dense_bond_energy_heisenberg,
        dense_entanglement_entropy, mps_expect_local, mps_expect_two_site,
        mps_bond_energy_heisenberg, mps_entanglement_entropy
  - scripts/run_heisenberg_benchmark.py: rendered markdown now ASCII-only
        (replaced U+00B7 middle dot and U+2248 to keep Windows default-locale
         read_text() safe in pytest)
  - docs/BENCHMARK_REPORT.md: regenerated by benchmark_score.py --fast
Commands run:
  - python scripts/validation_score.py --fast            -> exit 0 (Score: PASS, 36 passed)
  - python -m pytest -q tests/test_observables_dense_compare.py
                       tests/test_entanglement_entropy.py
                       tests/test_heisenberg_chi_sweep_smoke.py
                       tests/test_benchmark_score.py      -> 9 passed
  - python scripts/benchmark_score.py --fast             -> exit 0 (Benchmark score: PASS)
Results:
  - Stage 1 regression intact.
  - Stage 2 fast benchmark (tiny preset): N=4, chi=4, seed=0, steps=80
        exact_E0 = -1.61602540378, final_E = -1.61343377855,
        energy per bond = -0.537811259518, abs_err = 0.00259, PASS.
        Variational energy is ABOVE exact ground -> no below-ground violation.
  - Bethe thermodynamic reference recorded in report:
        1/4 - ln(2) ~= -0.4431471805599453 (trend only, not a finite-N target).
Current failing item: none
Next action: none (Stage 2 stop conditions met).
Notes:
  - MPS observables intentionally use mps.to_dense() internally (Stage-2
    correctness references, not efficient large-N contractions). A future
    Stage 3 may replace them with canonical-MPS contractions.
  - dense_entanglement_entropy detaches to numpy SVD; this is an observable on
    a converged/normalized state and is OUTSIDE the differentiable training
    energy path, so it does not violate the CLAUDE.md autograd rule.
  - Conventions unchanged from Stage 1: S = sigma/2, J = 1.0, open boundary,
    CPU, complex128. Pauli convention NOT used.
```
